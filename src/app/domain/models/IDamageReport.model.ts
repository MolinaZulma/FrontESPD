export interface IDamageReport {
  addressDamage:     string;
  descriptionDamage: string;
  image:             string;
  trueInformation:   string;
  typeDamage:        string;
  idUser:            string;
}
